export default function CardOpniao(props){
    return(
        <div>
            <p>${props.relato}</p>
            <p>${props.foto}</p>
            <p>${props.opniao}</p>
            <p>${props.cidade}</p>
        </div>
    )
}